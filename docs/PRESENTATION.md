# Презентация — требования, черновики слайдов, план

Шаблон организаторов: https://docs.google.com/presentation/d/17ciElVAWgeuPLiFHw7y5HMaAXmTo7g16
(37 слайдов, Montserrat, цвета #FF0053 #FFD6E4 #8A83D1 #FC3777 #310F53 #FFFFFF #1C1D22 #520978,
логотипы постановщиков на слайде 6). Экспорт в pptx: `…/export/pptx`.

**Слайды 7–11 строго обязательны и без изменений дизайна/структуры:**

| Слайд | Макет | Что заполнить |
|---|---|---|
| 7 | Титульный | название команды **ReSense**, название задачи (Кейс 05 · Обнаружение посторонних объектов в тоннеле метро по данным 3D-лидара), логотипы Московского транспорта / Метрополитена |
| 8 | Описание команды | капитан: ФИО, специальность; кол-во участников: 4; как образовалась команда; место работы/учёбы; город и регион; **краткое описание решения**; **в чём уникальность** |
| 9 | Команда | 4 карточки: Имя Фамилия, роль, ник в мессенджере, телефон, место работы/учёбы |
| 10 | История команды | 01 почему выбрали задачу; 02 что вдохновило; 03 сложности и как справились (+ личные истории) |
| 11 | Коротко о решении | техническая суть; маркетинговая суть (применение/развитие/внедрение) |

Далее (по рекомендации шаблона, слайды 12–29 как заготовки): подробное описание решения →
техническая проработка → уникальность → планы развития. Питч — меньший вес, чем техника (ТЗ §8.8),
но структура должна быть: **проблема → идея → алгоритм → демонстрация → результаты → что получилось**,
и главный кадр: «Вот поезд едет по тоннелю. Вот лидар. А вот препятствие, которое наш алгоритм увидел за X метров».

Правила для цифр в этом документе: каждое число помечено **[реальные данные]** (бэги
организаторов, `docs/EXPERIMENTS.md`, `docs/CAPTAIN.md` §0) или **[синтетика]** (объекты,
вставленные трассировкой лучей в реальные пустые кадры, либо полностью синтетический тоннель
из тестов и демо). Персональные данные — плейсхолдеры `<…>`, заполняет капитан.

## Заготовки формулировок

**Краткое описание решения.** ReSense — ROS 2-модуль, который по потоку 3D-лидара в реальном
времени строит модель «нормального тоннеля» (рельсы, полотно, ось пути с кривизной по стенам),
вырезает коридор габарита подвижного состава и сообщает «путь свободен» или «препятствие на X м»
с положением, размером и уверенностью.

**Уникальность.** Не классификация объектов, а геометрия среды: самокалибровка по рельсам,
кривизна пути по «параллельным ориентирам» (стены, ряды колонн) — коридор осмыслен и на 100+ м,
где рельсов уже не видно; честная зона доверия коридора; синтетические препятствия, вставленные
в реальные кадры трассировкой лучей самого лидара (правильная плотность и перекрытия) — обучение
и оценка без размеченных препятствий.

**Слайд «результаты» (заполнить из docs/EXPERIMENTS.md):** дальность первого обнаружения по
размерам объектов; recall по бинам дальности; FP/км на пустых бэгах; задержка/FPS на стенде;
сложные случаи (станции, стрелки, кривые) и что сделано. Черновик с цифрами — ниже, слайд 18.

---

## Слайды 7–11 — черновые тексты (строго по макету шаблона)

### Слайд 7 — Титульный

* Название команды: **ReSense**
* Название задачи: **Кейс 05 · Обнаружение посторонних объектов в тоннеле метро по данным 3D-лидара**
* Постановщики: Московский транспорт · ГУП «Московский метрополитен» (логотипы — со слайда 6 шаблона)
* Подпись (мелко): ЛЦТ 2026 · номинация «Компьютерное зрение»

### Слайд 8 — Описание команды

| поле шаблона | текст |
|---|---|
| Капитан | `<ФИО капитана>`, `<специальность / должность>` |
| Количество участников | 4 |
| Как образовалась команда | `<1–2 предложения; черновик:>` собрались под этот кейс из четырёх специализаций — системный анализ и ROS 2, фронтенд и визуализация, компьютерное зрение, данные и оценка; все четверо пишут пайплайн на Python, каждый в своей зоне |
| Место работы / учёбы | `<организация / вуз>` |
| Город и регион | `<город>`, `<регион>` |
| Краткое описание решения | ReSense — ROS 2-модуль, который по потоку 3D-лидара 10 раз в секунду строит модель «нормального тоннеля» (полотно, рельсы, ось пути с кривизной по стенам), вырезает коридор габарита подвижного состава и отвечает: «путь свободен» или «препятствие на X м» — с положением, размером и уверенностью. Без размеченных классов объектов, без GPU, в Docker |
| В чём уникальность | Мы описываем не объекты, а среду: самокалибровка по рельсам в каждом кадре, кривизна оси по «параллельным ориентирам» (стены, ряды колонн) — коридор осмыслен и там, где рельсов уже не видно; честная зона доверия коридора (дальше — только предупреждение); синтетические препятствия, вставленные в реальные кадры трассировкой лучей самого лидара, — оценка и настройка без размеченных препятствий |

### Слайд 9 — Команда (4 карточки)

| карточка | Имя Фамилия | роль | ник в мессенджере | телефон | место работы / учёбы |
|---|---|---|---|---|---|
| 1 (капитан) | `<Имя Фамилия>` | капитан · системный аналитик, разработчик робототехники (ROS 2, Docker, интеграция) | `<@ник>` | `<телефон>` | `<организация>` |
| 2 | `<Имя Фамилия>` | разработчик ПО · фронтенд и визуализация (RViz / Foxglove / веб-дашборд, видео, презентация) | `<@ник>` | `<телефон>` | `<организация>` |
| 3 | `<Имя Фамилия>` | инженер компьютерного зрения (модель пути, габарит, кластеризация, трекинг) | `<@ник>` | `<телефон>` | `<организация>` |
| 4 | `<Имя Фамилия>` | специалист по данным (датасет, синтетические препятствия, метрики, тесты, CI) | `<@ник>` | `<телефон>` | `<организация>` |

### Слайд 10 — История команды

**01 · Почему выбрали задачу.** Это задача безопасности с настоящими данными настоящего метро:
поезд без машиниста должен сам ответить «путь свободен» или «впереди препятствие, тормозить».
Нас зацепило, что организаторы прямо разрешили *не* строить очередной нейросетевой классификатор,
а описать нормальный тоннель и искать всё, что в него не вписывается, — задача про геометрию и
физику сенсора, а не про разметку.

**02 · Что вдохновило.** Сам лидар: Hesai Pandar128, 0,1° × 0,125° в рабочей зоне, стены тоннеля
возвращают точки на 150–200 м — значит, ось пути можно вести по стенам и рядам колонн там, где
рельсы уже не видны (так делают в задаче построения зоны пути для предупреждения столкновений
поездов, Shen et al., 2024). И отсутствие препятствий в данных: вместо того чтобы ждать
расширенный датасет, мы «поставили» человека и ящик в реальные кадры трассировкой лучей самого
лидара — с правильной плотностью точек и тенями. `<личная история участника, 1 фраза>`

**03 · Сложности и как справились.**
* **В данных нет препятствий в габарите** — шесть бэгов, 2 488 кадров, один человек, пересекающий
  путь на 55 м **[реальные данные]**. Ответ: `resense inject` — синтетические объекты в реальных
  кадрах с перекрытиями, и честная пометка каждого числа «реальное / синтетика».
* **Кривые, платформы и стрелки.** Прямой коридор упирается в стену на 50–130 м в трёх бэгах
  из шести; на платформе и стрелке — 49 из 67 остаточных ложных срабатываний **[реальные данные]**.
  Ответ: кривизна по стенам, зона доверия коридора, фильтры инфраструктуры; станции остались
  главной открытой проблемой — говорим об этом честно.
* **Бэги не совпадают между собой** (топик, `frame_id`, 120° против 360°, разная высота
  установки) — узел сам находит топик и калибруется по рельсам в каждом кадре.
  `<личная история: например, «первый прогон демо показал пустой экран — так мы узнали, что бэги разные»>`

### Слайд 11 — Коротко о решении

**Техническая суть.** ROS 2 Humble-узел (Python, numpy/scipy/scikit-learn, без GPU) на входе
берёт `PointCloud2`, на выходе публикует флаг препятствия, расстояние до ближайшего,
`Detection3DArray`, маркеры RViz и полный JSON-статус. Конвейер на кадр: модель пути (профиль
полотна, уровень головки рельса, ось из пары рельсовых гребней, рыскание/кривизна по стенам) →
полигон габарита, протянутый вдоль оси → кластеризация с масштабом, зависящим от дальности →
фильтры инфраструктуры → подтверждение за 3 кадра. 38–76 мс на кадр на 4 ядрах
**[реальные данные, песочница]**; человек на 55,6 м в `doubleT_obstacle` **[реальные данные]**.

**Маркетинговая суть.**
* *Применение:* функция «взгляд вперёд» для беспилотного поезда (GoA3/4) и для хозяйственных
  поездов в тоннеле; тот же модуль — для мониторинга габарита при обкатке линий.
* *Развитие:* накопление 5–10 кадров с компенсацией движения → 150–200 м для человека;
  калибровка на расширенном датасете с реальными препятствиями; полигон габарита по ГОСТ 23961-80.
* *Внедрение:* `docker build → docker run → ros2 bag play → результат`, один файл параметров,
  выход в стандартных сообщениях ROS 2 — стыкуется с существующим конвейером принятия решений
  без переделки.

---

## План слайдов 12–20 (проблема → идея → алгоритм → демонстрация → результаты → что получилось)

| № | слайд | содержание | материал |
|---|---|---|---|
| 12 | **Проблема** | Беспилотный поезд должен видеть путь: 100 м — хорошо, 200 — очень хорошо, 300 — отлично (ТЗ §8.2). Тоннель однороден, 99 % времени пуст, классов объектов нет — «найти человека на картинке» не подходит. Тормозной путь на 80 км/ч ≈ 190–250 м + реакция | ТЗ §1–2, `docs/RESEARCH.md` §6 |
| 13 | **Данные и сенсор** | 6 бэгов, 2 488 кадров, 250 с; Pandar128, ~190 тыс. точек/кадр, стены до 150–200 м, полотно до ~100 м. Физика: объект 0,5 м даёт ~7 точек на 100 м и 1,6 на 200 м — предел одного кадра. В данных нет препятствий в габарите **[реальные данные]** | `docs/DATASET.md`, `docs/SENSOR.md` |
| 14 | **Идея** | Описать *нормальный* тоннель и отметить всё, что не вписывается в габарит. Три опоры: самокалибровка по рельсам, кривизна по параллельным ориентирам, зона доверия | `docs/RESEARCH.md` §0, `docs/ALGORITHM.md` §1 |
| 15 | **Алгоритм** | Схема: облако → модель пути → коридор габарита → кластеры (ε растёт с дальностью) → фильтры инфраструктуры → подтверждение 3 кадра → выход. 5 параметров, которые меняют поведение (габарит, зона доверия, ε, confirm_hits, порог уверенности) | `docs/ALGORITHM.md` §3–5, `docs/ARCHITECTURE.md` |
| 16 | **Главный кадр** | «Вот поезд стоит в тоннеле. Вот лидар. А вот человек, которого наш алгоритм увидел за **55,6 м**» — рендер `doubleT_obstacle` #20: красный бокс, зелёная ось, синие предупреждения **[реальные данные]**. Второй кадр: тот же человек у ряда колонн → только предупреждение (#165) | `docs/img/doubleT_obstacle_0020.png`, `_0165.png` |
| 17 | **Демонстрация** | Цепочка организаторов: `docker build → run → ros2 bag play → RViz`; варианты показа: RViz в контейнере, Foxglove по сети (`web/foxglove_layout.json`), веб-дашборд с воспроизведением `results.jsonl`; видео 1–2 мин. Скриншот дашборда — синтетический подход человека 120 → 40 м **[синтетика]** | `README.md`, `web/README.md`, `docs/img/dashboard_synthetic.png`, видео (P2) |
| 18 | **Результаты** | таблица ниже; каждое число помечено | `docs/EXPERIMENTS.md`, `docs/CAPTAIN.md` §0 |
| 19 | **Сложные случаи / что не сработало** | платформа + стрелка (49 из 67 FP-кадров), кривые и переходы типа тоннеля, опора высоты за 80 м, объекты ниже 35 см, прореженная оценка занижает FP (на 10 Гц v0.3 тревожит ~половину кадров пустых бэгов) | `docs/EXPERIMENTS.md` §4, `docs/CAPTAIN.md` §0 |
| 20 | **Что получилось и что дальше** | рабочий ROS 2-модуль в Docker с тестами и CI; человек на 55,6 м на реальных данных; синтетика до ~100–115 м одним кадром; дальше — накопление кадров, расширенный датасет, ГОСТ-габарит, замер на i7-9700E | `docs/PLAN.md`, `docs/EVALUATION.md` §4 |

### Слайд 16 — «главный кадр», текст спикера

> Вот тоннель — двухпутный, поезд стоит, лидар на кабине. Вот облако: 350 тысяч точек за 0,1 с.
> Серым — стены и ряд колонн, зелёным — ось пути, которую алгоритм построил сам, по рельсам и
> колоннам. Оранжевым — всё, что попало в коридор габарита. И вот человек, который переходит
> путь: красный бокс, **55,6 метра**. Три кадра подряд — и поезд получает флаг «препятствие»
> и расстояние. Когда тот же человек отходит к колоннам, на 1,8 м от оси, — это уже не тревога,
> а предупреждение: коридор знает, где габарит кончается.

### Слайд 18 — результаты (черновик таблицы)

| показатель | значение | источник, пометка |
|---|---|---|
| Реальное препятствие: человек на пути, `doubleT_obstacle` (разметка `labels/doubleT_obstacle.json`: 71 кадр, где человек внутри габарита) | v0.5: обнаружен на **55,5–56,6 м** (по разметке 54,6–56,7 м), полнота **66 из 71** кадра (v0.3: 63), первая тревога в кадре 7 — через 0,5 с после входа в габарит (v0.3: кадр 9), ошибка дальности < 1 см, других тревог на этом бэге нет (v0.3: ещё 2 ложных события у ряда колонн) | **[реальные данные]** EXPERIMENTS §1 |
| Ложные тревоги на пустых бэгах, прореженные кадры (5 бэгов, 231 кадр, каждый 5-й/10-й) | v0.0 → v0.3: 149 → 92 → 76 → **67** кадров с тревогой, из них 49 в бэге «платформа + стрелка»; `roundT_squareT_pressureGate_squareT`: 0. Прореживание занижает FP — см. следующую строку | **[реальные данные]** EXPERIMENTS §1 |
| Ложные тревоги на пустых бэгах, **полная частота 10 Гц** (каждый кадр, 2 287 кадров пяти бэгов без препятствий) | v0.3: **1 001 кадр / 192 события** (`roundT_doubleT` 126/20, `roundT_pressureGate_roundT` 106/19, `doubleT_platform` 178/29, `roundT_squareT_pressureGate_squareT` 87/19, `squareT_platform_squareT_switch` 504/105) → **v0.5: 89 кадров / 29 событий** (0/0, 16/5, 12/3, 5/3, 56/18): ось пути по рельсам и кривизна 1/R по стенам, опора высоты только там, где полотно видно, сигнатуры инфраструктуры (колонна, навес, «висящий» объект, край, торцевая стена), подтверждение по времени и истории попаданий | **[реальные данные]** EXPERIMENTS §1, §1b |
| Ложные тревоги, что осталось в v0.5 | 29 событий на пяти бэгах: 18 — конструкция у торца платформы на 81–83 м, пока поезд стоит на станции (`squareT_platform_squareT_switch`), 5 — фрагменты лотка/шкафов у края габарита на 42–78 м в тоннеле с гермозатвором, 3 — одиночные далёкие кластеры на 101–127 м, 3 — столб на 85–87 м и два одиночных кадра на подходе к платформе; ни одной ложной тревоги в `roundT_doubleT` (v0.3: 20 событий) | **[реальные данные]** EXPERIMENTS §1 |
| Recall по дальности, объекты в реальных пустых кадрах `roundT_doubleT` | 0–50 м **3/3**; 50–100 м **1/2**; 100–150 м 0/5; 150–200 м 0/3; 200–300 м 0/3 (7 объектов из 26 полностью перекрыты и исключены) | **[синтетика]** EXPERIMENTS §2 (объекты трассировкой лучей в реальные кадры) |
| Дальность первого обнаружения | человек 68 / 80 / 110 м, ящик 0,5 м — 54–56 м | **[синтетика]** EXPERIMENTS §2 |
| Синтетический тоннель (тесты и демо) | ящик 0,6 м на 30 и 80 м, человек на 150 м (тесты); подход человека 120 → 40 м: подтверждён на **115,7 м**, ведётся до 40 м, 24 мс/кадр | **[синтетика]** `tests/`, `web/demo/make_demo_run.py` (песочница, 4 ядра) |
| Задержка и FPS | 38–76 мс среднее, 47–125 мс p95 на кадр (Python, 4 ядра, песочница); период кадра 100 мс → 10 Гц в реальном времени с запасом; на 347-тыс. кадрах 360° — 52 мс | **[реальные данные]** EXPERIMENTS §3, CAPTAIN §0 п. 2; замер на стенде i7-9700E — **не сделан** (P1, Sprint 2) |
| Ресурсы | CPU-only, numpy/scipy/scikit-learn; GPU не нужен | — |
| Предел сенсора | 200 м при 10 % отражения только на 32 «дальних» каналах у горизонта; 300 м — вне паспортной дальности для обычных целей | SENSOR §2–3 (паспорт Pandar128) |

Формулировка для слайда: «На реальных данных — единственное препятствие в датасете найдено на
55,6 м. На синтетике одним кадром — человек до ~100–115 м, дальше физика: 1–5 точек на объект.
Дорога к 150–200 м — накопление кадров, и она у нас в плане».

---

## Слайды капитана (черновик, капитан правит)

Материал: `docs/RESEARCH.md` §0, `docs/EXPERIMENTS.md` §1 (таблица эволюции) и §4,
`docs/CAPTAIN.md` §0. Отвечают на ТЗ §8.7: какие гипотезы рассматривались, какие эксперименты
проводились, что не сработало, почему выбран финальный вариант, какие компромиссы.

### Гипотезы, которые рассматривали

1. **Обученный 3D-детектор (PointPillars / CenterPoint)** — отклонён для v0: в данных ноль
   реальных положительных примеров, учить пришлось бы только на синтетике; по публикациям
   (Rail-BEV на OSDaR23) стандартные детекторы дают ~0–3 % AP дальше 100 м. Оставлен как
   «второе мнение» на 0–100 м, если появится расширенный датасет.
2. **Сравнение с картой / предыдущим проездом (change detection)** — требует сантиметровой
   локализации и повторного проезда; в бэгах нет ни одометрии, ни повторов. Не подходит для
   контрольного бэга, который команда не видела.
3. **Фоновая модель «ожидаемой дальности на луч»** (range-image аномалии) — дёшево и независимо
   от кластеризации, но само по себе срабатывает на кабелях, знаках, мокрых пятнах; нужен тот же
   габарит и та же персистентность. Отложено как дополнительный признак.
4. **Геометрическая модель среды + коридор габарита + кластеризация + подтверждение** — выбрана:
   не нужны размеченные классы, объясняема, обобщается на новые проезды (ТЗ §8.4), работает
   на CPU в реальном времени.

### Почему модель среды, а не детектор объектов

* Организаторы ставят вопрос «есть ли на пути что-то, чего там быть не должно», а не «что это»;
  класс объекта не влияет на решение «тормозить».
* Габарит подвижного состава — известная величина; всё, что внутри него и не является путём,
  полотном или известной инфраструктурой, — препятствие по определению. Это и есть спецификация
  функции безопасности.
* Стены и ряды колонн видны на 150–200 м, рельсы — на 30–40 м: ось пути по «параллельным
  ориентирам» продлевает коридор туда, куда детектор объектов не дотягивается по числу точек.
* Контрольный бэг команда не видит: геометрия среды переносится между проездами, внешний вид
  объектов — нет.

### Что не сработало (и что мы из этого узнали)

| попытка | что случилось | что сделали |
|---|---|---|
| фиксированный центр пути и прямоугольный коридор, «сырой» DBSCAN (v0.0) | 149 FP-кадров из 231; DBSCAN «взрывался» до 1,4 с на плотных ближних точках | самокалибровка по рельсам, воксели с нормировкой по дальности, зоны (v0.1: 92 FP, 32–75 мс) |
| прямой коридор | упирается в стену / ряд колонн на 50–130 м в трёх бэгах из шести | рыскание и кривизна по стенам (v0.2: 76 FP); «ближняя граница побеждает» при расхождении сторон (v0.3: 67 FP) |
| линейная экстраполяция высоты полотна | за ~80 м уходит на 1–2 м на 120 м → фантомные «подвесные» препятствия; 7 из 26 синтетических объектов оказались «перекрыты» из-за неверной опоры высоты | понижение «подвесных» кластеров до предупреждения; в плане — вторая опора высоты по основанию стен |
| один кадр дальше ~100–150 м | 1–5 точек на объект — физический предел плотности лучей | recall 0/11 дальше 100 м **[синтетика]**; накопление 5–10 кадров с компенсацией движения — главный пункт Sprint 2 |
| платформа + стрелка | 49 из 67 остаточных FP-кадров: край платформы с знаками на 0,1–0,3 м внутри коридора, поперечные конструкции на 40–130 м | фильтры «линейных боковых» и «стеноподобных» кластеров; станции — открытая проблема, говорим честно |
| оценка на прореженных кадрах | «каждый 10-й» вместо «каждый 5-й» превращал 8 тревожных кадров в 1: подтверждение за 3 кадра при прореживании требует секунды устойчивости вместо 0,3 с; на полной частоте v0.3 даёт тревогу примерно на половине кадров пустых бэгов (21.09) | правило: метрики только на полной частоте или с явной пометкой прореживания; FP считать событиями, а не кадрами; подтверждение мерить в секундах, а не в кадрах |
| «пустой экран» на демо-бэге | бэги не совпадают по топику и `frame_id` (`/lidar_points`/`hesai_lidar` против `/sensing/lidar/hesai128/pointcloud`/`lidar_livox`) | узел с автопоиском топика; RViz с фиксированной системой `resense_lidar` и статическим TF |

### Компромиссы

* **Подтверждение за 3 кадра** — 0,3 с задержки (6,7 м на 80 км/ч) в обмен на подавление
  однокадрового шума; первая тревога — уже подтверждённый объект.
* **Объекты ниже 35 см на шпалах отфильтрованы намеренно** (правило «низкая арматура»): иначе
  накладки, стыковые накладки и кабели дают тревоги; пересмотреть на расширенном датасете.
* **Зона доверия коридора**: за последней видимой границей стен — только предупреждение, не
  тревога; лучше честное «не знаю», чем ложная остановка.
* **Чистый Python**: 40–75 мс/кадр на 4 ядрах — реальное время с ограниченным запасом; накопление
  кадров может удвоить нагрузку (Numba/C++ — резерв).
* **Нет семантики**: стоящий состав, дрезина и рабочий на пути — одинаково «препятствие»; для
  функции безопасности это правильно.
